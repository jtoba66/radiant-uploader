{
  "name": "radiant-uploader",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build:fake": "echo \"faking build\" && cp -R src build",
    "build": "npm run build:fake"
  },
  "dependencies": {

  },
  "devDependencies": {

  }
}
